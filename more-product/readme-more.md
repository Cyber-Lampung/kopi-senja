<!-- if (id.includes(items.id)) {
const image = document.querySelectorAll("#image");

        image.forEach((item) => {
          try {
            item.src = "../../" + items.image;
          } catch {
            console.log("error");
          }
        });
      } else {
        console.log("err");
      } -->
